See https://github.com/lxc/incus/blob/main/doc/rest-api.yaml for the source of truth for the Incus REST API.

Be sure to add unit test for 100% coverage for any new functionality.

Be sure to run `npm run format` to keep code formatted consistently.
