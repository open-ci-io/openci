name: build_job_worker
description: Dart worker for executing OpenCI build jobs.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  http: ^1.6.0
  openci_shared: ^1.0.1
  sentry: ^9.22.0

dev_dependencies:
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.24.0
